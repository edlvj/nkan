h5.card-header= t('app.search')
.card-body
  form(action="/", method="GET")
    .input-group
      input.form-control(type="text" placeholder="Search for..." name="search")
      span.input-group-btn
        button.btn.btn-secondary(type="submit") Search