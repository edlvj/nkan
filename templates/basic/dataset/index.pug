extends ../layout

block content
  .container
    .row
      .col-md-8
        h1.my-4 DataSets
        each dataset in datasets
          include _dataset
        
        if datasets.length > 0
          include _pagination
        else
          h3 No datasets  
      .col-md-4
        .card.my-4
          include ../layout/_search
        .card.my-4
          include _categories
        .card.my-4
          include _datatype
